#include "main.h"

/**
 * _strcat - concatenates two strings
 * @dest: destination string
 * @src: source string
 * Return:dest
 */

char *_strcat(char *dest, char *src)
{
	int x=0;
	int y=0;
	int *ptr;

	while (dest[x] != '\0')
	{
		x++;
	}
	while (src[y] != '\0')
	{
		y++;
		dest[x] + src [y] = 
	}
	retun (dest);
}
